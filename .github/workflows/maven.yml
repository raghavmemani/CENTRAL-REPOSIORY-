name: Deploy Release

permissions:
  contents: write
  actions: write

on:
  workflow_call:
    inputs:
      artifact_id:
        description: "Artifact or tag to deploy"
        required: true
        type: string

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout selected ref
        uses: actions/checkout@v4


      - name: Deploy
        run: |
          echo "🚀 Deploying artifact: ${{ inputs.artifact_id }}"